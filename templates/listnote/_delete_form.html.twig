<form method="post" action="{{ path('app_listnote_delete', {'id': listnote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ listnote.id) }}">
    <button class="btn">Delete</button>
</form>
