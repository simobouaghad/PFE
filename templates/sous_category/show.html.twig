{% extends 'base.html.twig' %}

{% block title %}SousCategory{% endblock %}

{% block body %}
    <h1>SousCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sous_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sous_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sous_category_index') }}">back to list</a>

    <a href="{{ path('app_sous_category_edit', {'id': sous_category.id}) }}">edit</a>

    {{ include('sous_category/_delete_form.html.twig') }}
{% endblock %}
