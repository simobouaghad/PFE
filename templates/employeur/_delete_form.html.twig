<form method="post" action="{{ path('app_employeur_delete', {'id': employeur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employeur.id) }}">
    <button class="btn" style="background-color: red; color: #fff;">Delete <i class="icon-bin ml-2"></i></button>
</form>
